name: locale
description: "locale"
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  shared_preferences: ^2.3.2
  flutter_bloc: ^8.1.4
  intl: ^0.20.2
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.4.12
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_localizations:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true
flutter_intl:
  enabled: true
