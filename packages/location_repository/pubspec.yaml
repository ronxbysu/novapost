name: location_repository
description: location_repository
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  equatable: ^2.0.5
  http: ^1.2.0
  auth_repository: *auth_repository_version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.8

